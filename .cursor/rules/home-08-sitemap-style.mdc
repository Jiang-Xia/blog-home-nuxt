---
description: sitemap 静态抓取模块风格（按约定直取）
globs: modules/**/*.ts
alwaysApply: false
---

# sitemap 代码约定

- 文章列表/详情列表等接口按项目约定直接使用 `res.data.list`，再 `map` 出 `v.id` 生成 `/detail/${id}`，不要写 `pickList/pickArticleId` 之类兼容分支。
- 取消深层可选链与数组/对象的多重判空（如 `payload?.data?.list`、`Array.isArray(...)?...:...`、`filter(Boolean)`），除非明确无法命中接口约定。
- 保持代码轻量：避免大对象拼装、避免多余中间函数；优先用直观的 `map/concat`。
