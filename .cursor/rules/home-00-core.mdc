---
description: blog-home-nuxt 项目总览与变更原则
alwaysApply: true
---

# blog-home-nuxt 核心约定

## 技术栈

Nuxt 4 · Vue 3 Composition API · TypeScript · Pinia · Nuxt UI 4 · Tailwind CSS 4 · DaisyUI · Inspira UI

后端联调对象：`blog-server`，响应协议 `{ code, data, message }`。

## 目录职责

| 目录 | 用途 |
|------|------|
| `pages/` | 路由页面；页面私有组件放 `pages/<模块>/components/` |
| `layouts/` | `default` |
| `components/` | 按域划分：`xia/`（通用 UI）、`user/`、`rpg/`、`base/` |
| `composables/` | `useXxx` 可复用逻辑；跨组件共享态优先 `useState` |
| `api/` | 按业务拆分；统一走 `request.ts` |
| `types/` | 模块类型（如 `rpg.ts`）；全局类型放 `global.d.ts` |
| `utils/` | 纯工具函数（cookie、crypto、toast 等） |
| `config/` | 环境变量派生的 URL 与静态配置 |
| `modules/` | 自定义 Nuxt 模块（sitemap、inspria-ui） |
| `plugins/` | 客户端插件用 `.client.ts` 后缀（见 `home-13-plugins.mdc`） |
| `server/api/` | Nitro 服务端路由（见 `home-12-server-nitro.mdc`） |
| `stores/` | Pinia 全局 store（见 `home-11-stores-pinia.mdc`） |
| `middleware/` | 路由中间件 |

## 路径别名

- `~~/`、`~/`：项目根（api、composables、config）
- `@/`：同上，components/utils 中常用

## 变更原则

- 最小 diff：只改任务相关文件，不重构无关模块。
- 复用现有抽象：`xia-*` 组件、`useUserInfo`、`request.get/post`、DaisyUI class。
- SSR 安全：浏览器 API 包在 `import.meta.client` 或 `.client.vue` 内。
- 路由/layout 约束见 `home-10-pages-layout-routing.mdc`；新页面开发与样式见 `home-14-page-development.mdc`（详 `docs/page-development-guide.md`）。
- 注释约束见 `home-09-commenting-hard-requirement.mdc`。
- Pinia / Server / Plugins 专项见 `home-11` ~ `home-13`。
